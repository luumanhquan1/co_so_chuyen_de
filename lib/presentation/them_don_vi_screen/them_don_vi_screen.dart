import 'package:flutter/material.dart';
class ThemDonViScreen extends StatelessWidget {
  const ThemDonViScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return GestureDetector(
      onTap: (){

      },
      child: Container(
        height: 100,
        width: 100,
        color: Colors.red,
      ),
    );
  }
}
