import 'package:flutter/material.dart';

class LichHopTheoTuan extends StatelessWidget {
  const LichHopTheoTuan({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        height: 40.0,
        width: 40.0,
        color: Colors.red,
      ),
    );
  }
}
