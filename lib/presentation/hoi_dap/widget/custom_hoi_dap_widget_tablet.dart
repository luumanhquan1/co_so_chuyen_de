import 'package:flutter/material.dart';

class CustomHoiDapWidgetTablet extends StatelessWidget {
  const CustomHoiDapWidgetTablet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
